name: webpack

on:
  push:
    branches: [ "master" ]
  pull_request:
    branches: [ "master" ]

jobs:
  build:
    runs-on: ubuntu-latest
    
    steps:
    - name: Install
      run: |
        dir
        npm install
        npm start

    - name: Build
      run: npm run build
